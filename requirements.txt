fake_useragent
fp
free_proxy
# selenium-wire bundles old mitmproxy that imports blinker._saferef (removed in blinker 1.7+)
blinker==1.6.3
PyQt6>=6.6.0
requests
selenium
selenium_wire
# pkg_resources lives in setuptools <81; selenium-wire's bundled kaitaistruct still imports it.
setuptools<81
webdriver_manager
